{% extends 'base.html.twig' %}

{% block body %}
    <h1>Commexp</h1>

    <table>
        <tbody>
            <tr>
                <th>Idcommexp</th>
                <td>{{ commExp.idCommExp }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ commExp.comment }}</td>
            </tr>
            <tr>
                <th>Idexp</th>
                <td>{{ commExp.idExp }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('commexp_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('commexp_edit', { 'idCommExp': commExp.idCommExp }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
